<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.android.androidcourse2.DeliveryActivity"
    android:orientation="vertical"
    android:weightSum="100">


    <ImageView
        android:layout_width="fill_parent"
        android:layout_height="0dp"
        android:src="@color/colorPrimaryDark"
        android:id="@+id/imageView"
        android:layout_weight="30"
        android:contentDescription="paket" />

    <Button
    android:layout_width="match_parent"
    android:layout_height="0dp"
    android:layout_weight="10"
    android:onClick="uploadPhoto"
    android:id="@+id/uploadPhoto"
    android:text="@string/photoUpload"/>

    <EditText
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:inputType="textMultiLine"
        android:ems="10"
        android:id="@+id/deliveryComment"
        android:layout_weight="30"
        android:hint="@string/deliveryComment"
        android:enabled="true" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:text="@string/delivery_date"
        android:id="@+id/deliveryDate"
        android:layout_weight="10" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:id="@+id/deliveryLocation"
        android:layout_weight="10"
        android:text="@string/location"/>

    <Button
    android:layout_width="match_parent"
    android:layout_height="0dp"
    android:layout_weight="10"
    android:id="@+id/saveComment"
    android:onClick="saveComment"
    android:text="@string/saveDeliver"/>

</LinearLayout>
